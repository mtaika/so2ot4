generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./data.db"
}

model Ajo {
  id Int @id @default(autoincrement())
  reitti String
  km Int
  kayttaja Int
  pvm String
}